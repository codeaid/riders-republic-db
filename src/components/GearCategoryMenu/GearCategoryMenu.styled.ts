import styled from 'styled-components';

export const StyledGearCategoryMenu = styled.ul`
  display: grid;
  grid-gap: 0.25rem;
  list-style-type: none;
  margin: 0;
  padding: 0;
  user-select: none;
`;
